// This script will use the bootimage and use it to run QEMU
// This script will grab the artifact from build the kernel and convert it
// into a bootable image


use std::path::Path;
use std::process::Command;

fn main() {
    
}
